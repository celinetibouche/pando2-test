<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pando2 - Qualité de l'air</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns@3.0.0/dist/chartjs-adapter-date-fns.bundle.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  
  <header class="bg-white shadow-sm border-b-2 border-gray-900 py-6 px-8">
    <h1 class="text-3xl font-bold text-gray-900">PANDO2</h1>
  </header>

  <main class="container mx-auto px-8 py-8">
    
    <div class="bg-white rounded-lg shadow p-6 mb-8">
      <div class="flex items-center gap-8">
        <h2 class="text-lg font-semibold text-gray-700">Filtres</h2>
        
        <div class="flex-1">
          <label class="block text-sm font-medium text-gray-600 mb-2">Salles</label>
          <select id="roomFilter" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            <option value="all">Toutes les salles</option>
          </select>
          <p class="text-xs text-gray-500 mt-1">Représente la liste des salles présentes dans les fichiers CSV</p>
        </div>

        <div class="flex-1">
          <label class="block text-sm font-medium text-gray-600 mb-2">Paramètres</label>
          <select id="paramFilter" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            <option value="all">Tous les paramètres</option>
            <option value="co2">CO2</option>
            <option value="tmp">Température (TMP)</option>
            <option value="voct">VOCT</option>
          </select>
          <p class="text-xs text-gray-500 mt-1">Représente la liste des paramètres (temp, co2...)</p>
        </div>
      </div>
    </div>

    <div id="roomLegend" class="flex gap-6 mb-6 flex-wrap"></div>

    <div class="space-y-8">
      <div class="bg-white rounded-lg shadow p-6" id="co2Section">
        <h3 class="text-xl font-bold text-gray-800 mb-4">CO2</h3>
        <canvas id="co2Chart" height="80"></canvas>
      </div>

      <div class="bg-white rounded-lg shadow p-6" id="tmpSection">
        <h3 class="text-xl font-bold text-gray-800 mb-4">TMP (Température)</h3>
        <canvas id="tmpChart" height="80"></canvas>
      </div>

      <div class="bg-white rounded-lg shadow p-6" id="voctSection">
        <h3 class="text-xl font-bold text-gray-800 mb-4">VOCT</h3>
        <canvas id="voctChart" height="80"></canvas>
      </div>
    </div>

  </main>

  <%= javascript_include_tag "dashboard", "data-turbo-track": "reload", defer: true %>
</body>
</html>